# Ignore everything by default
*

# But do not ignore directories (so .nix files inside dirs are reachable)
!*/

# Allow all .nix files anywhere in the tree
!**/*.nix

# Quickshell config (QML)
!quickshell/
!quickshell/**

# Documentation
!README.md
!SHELL_CONCEPT.md

# Flake lock file should be tracked for reproducible builds
!flake.lock

# Keep the .gitignore itself
!.gitignore

# Optional common ignores
result
.cache
.DS_Store
*.pem
*.key
*.gpg
.env
node_modules/